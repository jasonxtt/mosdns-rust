# v0.1.11

## Summary

This release continues polishing the merged dashboard UI by making confirmation flows consistent across the page and improving button readability in the `SOCKS5 / ECS IP` section.

## Changed

- unified confirmation interactions in the merged dashboard so destructive and important actions now use the same inline popover style instead of mixed native dialogs
- updated the `SOCKS5 / ECS IP` action buttons for clearer contrast and aligned the `保存` button with the main primary-button styling

## Config impact

This release does **not** require a config change for existing users.

No incremental config package update is needed for this release.

## Notes

- this release only updates the source-side WebUI
- legacy compatibility routes such as `/legacy` remain unchanged
