# v0.3.14

## Summary

This release adds a configurable `IPv4优先` path to the maintained Vue dashboard and aligns the runtime routing flow so the setting works consistently across direct queries, requery refreshes, and dedicated upstream groups.

- the system settings page now exposes an `IPv4优先` toggle
- `IPv4优先` and `IPV6屏蔽` are now treated as mutually exclusive operator modes in the UI
- runtime config sequencing was adjusted so dedicated upstream matching also benefits from `prefer_ipv4`, including the `sequence_requery` path

## Added

- added an `IPv4优先` switch in the Vue system settings page
- added an inline UI hint when enabling one IPv4/IPv6 mode auto-disables the conflicting mode

## Changed

- the maintained `/` dashboard now treats `IPv4优先` and `IPV6屏蔽` as mutually exclusive toggles
- `prefer_ipv4` is now expected to run after rule matching and dedicated-upstream marking, so earlier routing branches such as DDNS and special upstream groups can still benefit from IPv4 preference
- the requery path now mirrors the main query path by applying dedicated-upstream matching before `prefer_ipv4`

## Config impact

This release **does** require updated runtime YAML if you want the new `IPv4优先` behavior to apply correctly.

The updated config package changes:

- `config_custom.yaml`

Existing deployments should update their config package together with the binary for full behavior parity.
