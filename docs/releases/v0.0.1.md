# v0.0.1

First public release for `jasonxtt/mosdns-rust`.

## Highlights

- Adds hybrid Rust cache integration (`Go host + Rust cache core`).
- Keeps existing mosdns configuration compatibility.
- Provides Linux release artifacts via GitHub Actions.

## Release Artifacts

- `linux-amd64`
- `linux-amd64-v3`
- `linux-arm64`
- `linux-armv7`
