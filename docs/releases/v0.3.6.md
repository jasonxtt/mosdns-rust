# v0.3.6

## Summary

This release fixes the overview `查询趋势` card so panel transparency and blur settings affect it consistently with other modules.

## Changed

- overview trend card:
  - aligned `查询趋势` container background with panel transparency/glass variables
  - fixed mismatch where trend card kept a fixed background while other modules followed theme transparency
- embedded dashboard asset version:
  - updated `log.html` asset query string to force browser refresh of updated CSS/JS bundle references

## Config impact

This release does **not** require a config change for existing users.

No incremental config package update is needed for this release.
